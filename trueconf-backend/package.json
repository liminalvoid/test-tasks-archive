{
  "name": "basic-shapes",
  "version": "1.0.0",
  "description": "Basic shapes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "compile": "tsc"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
